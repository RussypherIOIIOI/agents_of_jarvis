"""agent-hub-core: shared hardened core for JARVIS agents.

Provides the sandboxed executor, model-agnostic LLM client layer, and
structured tracing that every agent in agents-of-jarvis depends on.
"""
